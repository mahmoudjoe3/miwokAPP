<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahmoudjoe333.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.PhrasesActivity"
            android:label="@string/phrases"/>

        <activity android:name=".ui.colorsActivity"
            android:label="@string/colors"/>

        <activity android:name=".ui.familyMembersActivity"
            android:label="@string/family"/>

        <activity android:name=".ui.AllActivity"
            android:label="@string/number"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>